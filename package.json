{
  "name": "neo-kit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "deploy": "next build && next export -o docs && touch docs/.nojekyll && git add docs/  && git commit -m \"Deploy Next.js to gh-pages\"  && git subtree push --prefix docs origin gh-pages",
    "start": "next start"
  },
  "dependencies": {
    "@popperjs/core": "^2.10.1",
    "next": "10.2.0",
    "next-images": "^1.8.1",
    "react": "17.0.2",
    "react-code-blocks": "0.0.9-0",
    "react-dom": "17.0.2",
    "react-select": "^5.1.0",
    "sass": "^1.41.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.2.5",
    "postcss": "^8.2.14",
    "tailwindcss": "^2.1.2"
  }
}
